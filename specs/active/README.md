# Active Specs

Specs currently being implemented go here. Move from `specs/future/` when work starts, move to `specs/completed/` when shipped. See [specs/index.md](../index.md) for the full lifecycle and templates.

_(empty — no spec currently in flight)_
