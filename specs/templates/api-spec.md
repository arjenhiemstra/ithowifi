# API Spec: <name>

Status: Draft | Active | Completed
Related ADR(s):

## Summary

What API surface is being added/changed: REST v2 endpoint(s), MQTT topic(s), or WebSocket message(s).

## Contract

### REST (if applicable)

| Method | Path | Request | Response |
|---|---|---|---|
| GET/POST | `/api/v2/...` | ... | JSend via `ApiResponse` |

The served OpenAPI spec is generated by `build_openapi_spec()` in `software/NRG_itho_wifi/build_script.py` (into `main/webroot/openapi_json_gz.h`) — edit the Python spec builder, don't hand-edit generated output. Keep the standalone `docs/openapi.json` in sync separately.

### MQTT (if applicable)

| Topic | Direction | Payload |
|---|---|---|
| `<base>/...` | publish/subscribe | ... |

### Implementation shape

Logic goes in a `processXxx` function (`main/api/WebAPIv2.cpp`); REST binding in `handleXxx` (`WebAPIv2Rest.cpp`); MQTT binding in `MqttAPI.cpp`. See [.ai/common-patterns.md](../../.ai/common-patterns.md) #3.

## Acceptance Criteria

- [ ] Endpoint/topic behaves per contract above
- [ ] OpenAPI spec updated and matches (`tests/api/test_openapi_completeness.py`, `test_openapi_spec.py` if extending those suites)

## Architecture Impact

REST/MQTT API surface changes are architecturally significant by definition — an ADR is required unless this is a pure additive/backward-compatible extension of an existing endpoint family. See [.ai/ai-instructions.md](../../.ai/ai-instructions.md).

## Risks

- Backward compatibility for existing integrations (Home Assistant, Homey, Domoticz — see [.ai/project-overview.md](../../.ai/project-overview.md))

## Test Requirements

- [ ] `test_native_api_validation` / `test_native_mqtt_cmd_parse` extended for new validation logic
- [ ] `tests/api/` or `tests/mqtt/` case against live hardware

## Rollback Strategy

Can the API change ship behind additive-only semantics (new fields/topics, nothing removed) to make rollback a non-event? If not, document the breaking-change migration path.

## Related Components

File/module list this touches.
